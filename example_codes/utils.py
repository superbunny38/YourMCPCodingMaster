def create_random_message(name = "Ch"):
    """
    Create a random message with a given name.
    
    Args:
        name: The name to include in the message.
    
    Returns:
        A string containing the random message.
    """
    return f"Hello {name}, this is a random message!"